import React from "react";
import { Text, Container } from "../../../components/Styles";

interface CreateSeedProps {}

export const CreateSeed: React.FC<CreateSeedProps> = ({}) => {
  return (
    <Container>
      <Text>Create Seed</Text>
    </Container>
  );
};
